function CMC = compute_cmc(eeg_signal,emg_signal)
%SPECTRAL_POWER Summary of this function goes here
%   Detailed explanation goes here

%% DENOISING

%% SEGMENTATION
DUR = 4; % Duration of each segment (1-4s)

print(eeg_signal.length / DUR);

%% POWER SPECTRUM COMPUTATION
S_x = 0;
S_y = 0;
S_xy = 0;
%% OUTPUTS
CMC = abs(S_xy)^2 / (S_x * s_y);
end

